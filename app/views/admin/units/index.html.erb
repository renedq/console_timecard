  <%= link_to 'Add Unit', new_admin_unit_path, class: 'btn btn-primary' %>
<div class='page-header'>
	<table class='table table-striped table-hover' width=40%>
		<thead>
			<tr>
				<th><br></th>
				<th>Name</th>
				<th>City</th>
				<th>State</th>
				<th>Members</th>
			</tr>
		</thead>

		<tbody>
			<% @units.each do |unit| %>
				<tr>
					<td><%= link_to 'Edit', edit_admin_unit_path(unit.id) %></td>
                                        <td><%= link_to unit.name, admin_unit_path(id: unit.id) %></td>
					<td><%= unit.name %></td>
					<td><%= unit.city %></td>
					<td><%= unit.state %></td>
					<td><%= unit.users.count %></td>
				</tr>
			<% end %>
		</tbody>
	</table>
</div>
